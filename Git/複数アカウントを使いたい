前使ってた別のアカウントでせっていしたことが邪魔になってpushできなかった。
色々調べてわかったことをまとめておく

重要だったのはgitをローカルで設定した時に売ったコマンド
git config --global なんちゃら　の意味を理解すること。

このコマンドでは、gitのアカウント名とかメールアドレスを登録している
最後にこのコマンドで設定したこと(アカウント名、等）が適用される。

したがって、昔の設定を上書きしてしまえば済むことだったのだが、
実際には”複数のアカウントを1つのアカウントで使いたい”ということも
あるだろうから調べた。

まず、git cofig ﾎﾆｬﾗﾗ　で打ったコマンドはconfigﾌｧｲﾙや、.gitconfigﾌｧｲﾙ
に書き込まれる。２つの違いは、保管場所と役割。
  それぞれまとめる
    configﾌｧｲﾙ　= local
      場所：<リポジトリ名>/.git/config
      役割：リポジトリごとの設定（アカウント名とか）
    .gitconfigﾌｧｲﾙ = global
      場所: C:¥Users¥[ユーザー名]¥.gitconfig (macだったら、~/.gitconfig)
      役割: ユーザー内共通の設定
   優先順位は　local > global となる。
   
このことより、pushできなかった原因は古いほうのアカウントの設定を使って
いたことだと分かった。
したがって、ローカルのconfigﾌｧｲﾙに新しく設定を書き込んであげたら解決。
  


と思ったら小さいエラーが出たのでそれも直す必要があった。
  内容：ローカルで設定したアカウントでじゃなくてグローバルのほうでpushしようとするからエラーが出る。
  原因は詳しくわからなかったが、configﾌｧｲﾙを以下のようにいじったら直すことができた。
  おそらくパスワードの部分は一回pushできたら消してしまっても大丈夫だと思うので後で試してみる。

＃githubのwebサイトから追記
[remote "origin"]
 url = https://username:password@github.com/xxxxxx/demo-repo.git

これをgithubTest/.git/config に追記。(似たようなのがはいいてたら書き換え）
コマンド打ってもいいけどこっちのほうが早い

参考にしたqiita
  githubにpushする時に勝手に別ユーザでpushされる現象の対処法　
    https://qiita.com/comefigo/items/8acfc82b1dbbc314d90d
  //これ調べたらドンピシャ





その他調べて参考になったこと。
  git pushで403エラーが返された話
    https://qiita.com/F-S/items/6673ad4a5ccef9d84a68
  //エラーの原因がこれで分かった

  複数のgitアカウントを使用する場合
    https://qiita.com/yamataku29/items/4744c9c70ad793c83b82
  //ssh接続の記事だけど参考になった(ただしちょっと古いやり方かも、、、）

  複数のgitアカウントを使い分ける
    https://qiita.com/0084ken/items/f4a8b0fbff135a987fea
  //よく使うアカウントはグローバル、あまり使わないアカウントはリポジトリごとに登録
    するというのが参考になった
