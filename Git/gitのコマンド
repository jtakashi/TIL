基本
  git add
  git commit
  git push
  git clone
  git pull
  
使ったのを忘れないうちに
  git merge  
  git fetch           fetch + merge = pull ということらしい
  
  git branch　        自分がいまいるブランチを調べたいとき
  
  git config --list   リポジトリの設定を確認　ユーザー名とか　--globalや--localで絞込できる
  
  git status          addの状況が見れる
  
  git log             コミットの履歴が見れる
  
  git reflog          ブランチ切り替えとかマージの履歴とかが見れる。
  
  その他
 【Git入門】基本操作の次に覚えておきたい便利な機能5選 (https://liginc.co.jp/196337/2)    
   ｇit stash とかの解説あり 
  
  0420
  git branch -r 　                   リモート追跡ブランチを確認
  git fetch origin/<ブランチ名>      追跡中のリモートブランチの更新　mainブランチだったらfetchだけでたぶん行ける
  git check out -b <新しいブランチ>  ブランチの切り替え　-bオプションでブランチの作成と同時に切り替え
  git branch -m                     <古いブランチ名><新しいブランチ名> ブランチ名の変更
  git branch -vv                    ブランチが紐づいているリモートブランチを確認
  
 
 
